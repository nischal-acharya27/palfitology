# Examples

This folder will eventually carry a tiny self-contained sample (one or two
objects' worth of FITS cutouts and a matching catalog) that lets you smoke-
test a fresh palfitology install with:

```bash
cd examples/
palfitology fit-pa --all --workers 2
```

For now, see `docs/examples.md` for usage patterns against your own data.
